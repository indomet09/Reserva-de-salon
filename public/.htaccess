RewriteEngine On

# Si el archivo o directorio existe, usar ese
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirigir todo a index.php
RewriteRule ^(.*)$ index.php [L,QSA]

# Prevenir directory listing
Options -Indexes
